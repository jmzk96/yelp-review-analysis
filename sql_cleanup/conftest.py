# this empty package fix pytest not finding the src folder
# otherwise you could add the following lines on top of each test file
# import sys
# import os
# sys.path.append(os.path.join(os.path.dirname(__file__), "..", "src"))
